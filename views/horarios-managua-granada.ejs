<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css-code/horariosRivas.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
  <div class="container">
    <h1 class="title"><%= title %></h1>
    <% if (horarios.length > 0) { %>
      <ul class="horarios-list">
        <% horarios.forEach(horario => { %>
          <li class="horario-item">
            <div class="route">
              <span class="origin"><%= horario.terminal_origen %></span>
              <span class="arrow">➔</span>
              <span class="destination"><%= horario.terminal_destino %></span>
            </div>
            <div class="details">
              <span class="label">Salida:</span> <%= horario.hora_salida %><br>
              <span class="label">Duración:</span> <%= horario.duracion_viaje %>
            </div>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p class="no-horarios">No hay horarios disponibles para esta ruta en este momento.</p>
    <% } %>
    <a href="/viajandoPorGranada" class="back-btn">Volver</a>
  </div>
</body>
</html>
